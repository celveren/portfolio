name: portfolio
services:
  website:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:${CELVEREN_PORT:-8080}:80"
    develop:
      watch:
        # Rebuild Tailwind and the site together; .dockerignore filters this path.
        - action: rebuild
          path: .
